---
description: server 安全与稳定性基线（鉴权、越权、异常、并发）
globs: server/**/*.go
alwaysApply: false
---

# Server 安全与稳定性规则

## 鉴权与越权

- 需要登录的路由必须挂载 `JWTAuth`，尤其是 WebSocket 入口。
- 涉及用户数据的查询/修改/删除，优先使用 token 中的用户身份，不信任前端传入 `userId`。
- 涉及资源 ID（如用户、群组、好友）时，必须做“当前用户是否有权限操作”校验。

## 异常处理

- 禁止在业务请求链路中使用 `panic` 处理可预期错误。
- 禁止忽略关键错误（网络请求、文件上传、JSON 解析、Redis/Gorm 操作）。
- 错误响应后必须 `return`，避免逻辑穿透。

## 并发与资源

- 共享 `map` 在多 goroutine 场景必须加锁或使用并发安全结构。
- 外部 HTTP 请求要考虑超时、判空与 `Body.Close()` 生命周期。
- 避免无 sleep 的忙等循环，防止 CPU 空转。

## 敏感信息

- 禁止打印密钥、密码、token、完整连接串等敏感配置到日志。
- 配置加载失败要快速失败，不允许半初始化继续运行。
