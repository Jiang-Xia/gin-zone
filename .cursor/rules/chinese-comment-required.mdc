---
description: 新增功能需补充简短中文注释（关键变量与方法）
alwaysApply: true
---

# 中文注释要求（强制）

- 新增或重构功能时，关键变量与关键方法必须加上简短中文注释。
- 注释重点说明“做什么/为什么这样做”，避免无信息注释。
- 注释保持简洁，通常 1 行即可，不要大段重复代码语义。
- 常量、配置项、重试标记、会话/鉴权相关字段，优先补注释。
- 对外方法、核心流程方法（如请求拦截、重试、加解密）必须有注释。

## 注释格式约束

- 允许直接写中文句子说明原因/意图，例如：`// token 失效后清理会话并跳转登录页`
- 不要使用固定前缀 `中文注释`（避免出现类似 `// 中文注释 ...` 这种重复样式）。
