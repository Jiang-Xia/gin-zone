---
description: JS 写法偏好（数值、判空），与现有页面脚本一致
globs: "**/*.js"
alwaysApply: false
---

# JavaScript 写法约定（client）

## 数值

- 在**已有约定**的场景（接口返回字段类型稳定、或 store 已统一规范化）对数字字段**直接使用/赋值**即可，避免在页面到处写 `Number(...)`、`parseInt`、`replace(/,/g,'')` 等重复转换。
- 若确需规范化（千分位字符串、持久化字符串等），**集中在一处**（例如单个 util、单个 store getter/动作、或展示层组件），不要在多处复制同一套转换。

## 判空

- 除必须区分 `0/false/''` 等合法 falsy 的情况外，优先用 `if (!x)` / `if (x)`，避免冗长的 `x === null || x === undefined` 等等价写法。
- 需要明确区分 `null` 与 `''`、或仅排除 `undefined` 时，再写对应的显式条件。
